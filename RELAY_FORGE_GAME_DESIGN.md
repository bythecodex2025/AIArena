# Relay Forge — AIArena 예제 게임 기획서

> 규칙 버전 1.2.0 — 누적 신호 점수, 전체 상태 초패, 교착 정산으로 반복 없는 12~14반수 경기를 목표로 한다.

## 1. 게임 개요

**Relay Forge**는 5×5 회로판에서 자신의 코어로부터 노드망을 확장하고, 중앙 릴레이를 장악하거나 상대 코어를 해킹하는 2인용 턴제 연결 전략 게임이다.

플레이어는 매 턴 다음 중 하나를 선택한다.

- 빈칸에 노드를 연결한다.
- 상대 노드를 과부하시켜 제거한다.
- 중요한 노드를 강화한다.
- 상대 코어를 해킹한다.
- 에너지를 아끼기 위해 대기한다.
- 후공이라면 첫 행동 직후 한 번 진영을 교환할 수 있다.

게임에는 주사위, 카드 셔플과 명중 확률이 없다. 같은 상태에서 같은 행동을 선택하면 항상 같은 결과가 나온다.

### 한 줄 소개

> 연결하고, 끊고, 중계기로 신호 5점을 모아라. 또는 상대 코어를 한 번 해킹하면 승리한다.

### 기본 정보

| 항목 | 내용 |
|---|---|
| 장르 | 완전 정보 2인 턴제 연결·영역 전략 |
| 보드 | 5×5, 25칸 |
| 일반 경기 길이 | 12~20반수 목표 |
| 최대 길이 | 48반수 |
| 무작위성 | 없음 |
| 평균 합법 행동 수 | 약 6~25개 목표 |
| AI 함수 | `chooseAction(state, me)` |
| 사용자 AI 제한 | 코드 50KB, 행동당 500ms |

## 2. 핵심 재미와 설계 목표

Relay Forge의 핵심은 **내 연결을 늘리는 행동과 상대의 연결을 끊는 행동 사이의 선택**이다.

1. **쉬운 입문**: 노드는 상하좌우로 이어 붙이면 된다.
2. **명확한 전술**: 두 방향 이상에서 둘러싼 상대 노드를 제거할 수 있다.
3. **장기 계획**: 중앙 릴레이를 유지할지 상대 코어까지 길을 낼지 선택한다.
4. **자원 판단**: 확장은 싸지만, 과부하·강화·해킹을 위해서는 에너지를 모아야 한다.
5. **AI 학습성**: 연결성, 중앙 장악, 절단점, 에너지와 승리 위협을 평가 함수로 발전시킬 수 있다.

## 3. 보드와 초기 상태

### 3.1 좌표

- 보드 크기: 5행 × 5열
- 인덱스: `index = row * 5 + column`
- 행과 열: `0`부터 `4`
- 인접: 상하좌우 네 방향만 사용
- 대각선 연결과 보드 밖 연결은 허용하지 않음

### 3.2 특수 칸

| 종류 | 위치 | 역할 |
|---|---|---|
| P2 코어 | `2` | P2 연결망의 시작점 |
| 릴레이 A | `5` | 좌측 신호 거점 |
| 릴레이 B | `12` | 중앙 신호 거점 |
| 릴레이 C | `19` | 우측 신호 거점 |
| P1 코어 | `22` | P1 연결망의 시작점 |

릴레이 칸은 노드를 설치할 수 없는 중립 시설이다. 코어 칸에도 다른 노드를 설치할 수 없고 코어는 제거되지 않는다.

### 3.3 초기 배치

각 플레이어는 코어와 연결된 기본 노드 두 개를 가지고 시작한다.

```text
     0       1       2       3       4
0    ·      P2      C2      P2       ·
1   RA       ·       ·       ·       ·
2    ·       ·      RB       ·       ·
3    ·       ·       ·       ·      RC
4    ·      P1      C1      P1       ·
```

- `C1`, `C2`: 코어
- `P1`, `P2`: 해당 플레이어의 기본 노드
- `RA`~`RC`: 노드를 놓을 수 없는 중립 릴레이
- P1 기본 노드: `21`, `23`
- P2 기본 노드: `1`, `3`
- 초기 에너지: 양측 `2`
- P1부터 시작하며, 각 플레이어의 첫 행동 전에도 정상적으로 에너지 1을 얻는다.

## 4. 연결과 전력

### 4.1 전력 공급 노드

자신의 코어에서 상하좌우로 이어진 자기 노드만 **전력 공급 상태**가 된다.

규칙 엔진은 각 상태에서 플레이어별로 다음 순서로 전력망을 계산한다.

1. 해당 플레이어의 코어를 시작점으로 둔다.
2. 상하좌우로 인접한 자기 노드를 탐색한다.
3. 이어진 모든 노드를 `poweredNodeIds`에 넣는다.

상대가 중간 노드를 제거하면 코어와 끊어진 뒤쪽 노드는 보드에 남지만 전력을 잃는다.

### 4.2 전력을 잃은 노드

비활성 노드는 다음 제약을 받는다.

- 새 노드를 연결하는 출발점이 될 수 없다.
- 릴레이를 제어하지 못한다.
- 상대 코어 해킹 조건에 사용할 수 없다.
- 과부하에 필요한 포위 노드 수에 포함되지 않는다.
- 다시 코어와 연결되면 즉시 전력을 회복한다.

비활성 노드도 강화 상태를 유지하며 상대 칸 점유물로 취급한다.

## 5. 턴과 에너지

### 5.1 턴 순서

한 반수는 다음 순서로 처리한다.

1. 현재 플레이어가 에너지 1을 얻는다.
2. 에너지는 최대 4를 넘지 않는다.
3. 엔진이 `legalActions`를 생성한다.
4. 플레이어 또는 AI가 행동 하나를 고른다.
5. 행동을 적용하고 전력망을 다시 계산한다.
6. 릴레이 제어와 누적 신호 점수를 갱신한다.
7. 승리, 반복과 최대 반수를 판정한다.
8. 종료되지 않았으면 상대 차례로 전환한다.

### 5.2 에너지

| 항목 | 값 |
|---|---:|
| 초기 에너지 | 2 |
| 자기 행동 시작 시 획득 | 1 |
| 최대 에너지 | 4 |

에너지는 플레이어별로 보관한다. 사용하지 않은 에너지는 다음 자기 차례로 이월된다.

## 6. 행동 규칙

한 반수에는 다음 행동 중 정확히 하나만 수행한다.

### 6.1 연결 `extend` — 비용 1

전력 공급 중인 자기 코어 또는 노드와 상하좌우로 인접한 빈칸에 새 노드를 설치한다.

```javascript
{ id: "extend:17", type: "extend", to: 17 }
```

- 목적지는 보드 안의 빈칸이어야 한다.
- 코어 칸에는 설치할 수 없다.
- 릴레이 칸에는 설치할 수 없다.
- 새 노드에는 경기 중 유일한 ID가 부여된다.
- 새 노드는 설치 즉시 전력 공급 상태가 된다.

### 6.2 과부하 `overload` — 비용 2

전력 공급 중인 자기 노드로 상대 노드를 둘러싸 제거한다.

```javascript
{ id: "overload:P2-n7", type: "overload", targetId: "P2-n7" }
```

과부하 조건은 다음과 같다.

- 대상은 상대의 일반 노드여야 한다.
- 대상과 상하좌우로 인접한 전력 공급 자기 노드를 센다.
- 일반 노드는 2개 이상, 강화 노드는 3개 이상 인접해야 한다.
- 조건을 만족하고 에너지가 2 이상이면 합법 행동이다.
- 성공하면 대상 노드를 즉시 보드에서 제거한다.
- 코어는 과부하할 수 없다.

한 노드를 제거해 여러 노드를 동시에 비활성화하는 **절단 전술**이 이 게임의 핵심이다.

### 6.3 강화 `fortify` — 비용 2

전력 공급 중인 자기 일반 노드 하나를 강화한다.

```javascript
{ id: "fortify:P1-n4", type: "fortify", nodeId: "P1-n4" }
```

- 코어와 이미 강화된 노드는 선택할 수 없다.
- 비활성 노드는 강화할 수 없다.
- 강화 노드를 제거하려면 상대 전력 노드 3개의 포위가 필요하다.
- 강화는 제거되기 전까지 유지된다.

### 6.4 코어 해킹 `hack` — 비용 3

상대 코어에 인접한 전력 공급 자기 노드가 있으면 상대 코어를 해킹해 즉시 승리한다.

```javascript
{ id: "hack:P2-core", type: "hack", targetId: "P2-core" }
```

- 상하좌우 인접만 인정한다.
- 해킹에 사용하는 노드는 전력 공급 상태여야 한다.
- 에너지가 3 이상이어야 한다.
- 코어에는 체력이 없으며 합법적인 해킹 한 번으로 경기가 끝난다.

### 6.5 첫 수 진영 교환 `swap` — 비용 0

P1의 첫 행동 직후 P2의 `legalActions`에만 한 번 나타난다.

```javascript
{ id: "swap", type: "swap", cost: 0 }
```

- 보드를 180도 회전하고 P1·P2의 코어, 노드, 강화 상태, 에너지와 ID를 교환한다.
- P2는 P1이 둔 첫 수를 자신의 배치로 넘겨받고, 다음 차례는 P1이 진행한다.
- P2가 다른 행동을 선택하면 이후 `swap`은 다시 제공되지 않는다.
- 선공의 특정 첫 수가 필승이라면 후공이 그 수를 가져갈 수 있으므로, 선공만 사용할 수 있는 절대우월 첫 수를 제거한다.

### 6.6 대기 `wait` — 비용 0

보드 상태를 바꾸지 않고 행동을 종료한다.

```javascript
{ id: "wait", type: "wait" }
```

대기는 과거 전체 상태를 그대로 재현하지 않을 때 합법이다. 과부하·강화·해킹을 위해 에너지를 모으거나 불리한 확장을 피할 때 사용한다.

### 6.7 교착 정산 `settle` — 비용 0

초패 규칙을 적용한 뒤 새로운 전체 상태를 만드는 행동이 하나도 없을 때만 제공된다.

```javascript
{ id: "settle", type: "settle", cost: 0 }
```

선택 즉시 신호 점수, 릴레이, 전력 노드, 전체 노드, 에너지 순서로 우위를 정산하고 경기를 끝낸다.

## 7. 릴레이 제어와 승리 조건

### 7.1 릴레이 제어

릴레이 `5`, `12`, `19` 주변의 전력 공급 노드 수를 플레이어별 영향력으로 계산한다.

- 영향력이 2 미만이면 제어자 없음
- 양측 영향력이 같으면 경합 상태이며 제어자 없음
- 한쪽 영향력이 2 이상이고 상대보다 높으면 그 플레이어가 제어
- 비활성 노드는 영향력에 포함하지 않음

### 7.2 누적 신호 점수 승리

각 플레이어는 자기 행동 직후 자신이 제어하는 릴레이 수만큼 `signalScores`를 얻는다. 획득한 점수는 제어권을 잃어도 사라지지 않는다.

- 릴레이 1개 제어: 행동 뒤 1점
- 릴레이 2개 제어: 행동 뒤 2점
- 승부 판정: P2의 응답까지 끝난 라운드 경계
- 한쪽만 5점 이상이면 그 플레이어 승리
- 같은 라운드에 양쪽이 같은 점수로 5점 이상이면 무승부

P1이 먼저 행동해도 P2에게 같은 라운드의 응답과 득점 기회를 보장하므로 단순 턴 순서에 의한 선취 편향을 막는다.

### 7.3 코어 해킹 승리

합법적인 `hack` 행동을 적용하면 다른 판정보다 먼저 즉시 승리한다.

### 7.4 전체 상태 초패와 교착 정산

다음 값을 포함한 과거 상태를 그대로 만드는 행동은 `legalActions`에서 제외한다.

- 보드의 코어·노드·강화 상태
- 양측 에너지
- 양측 누적 신호 점수
- 현재 플레이어

노드의 생성 순번처럼 전략 결과에 영향을 주지 않는 값은 상태 키에서 제외한다. 과부하 후 같은 칸 재설치나 자원 변화 없는 대기로 이전 상태가 되돌아가는 순환을 원천 차단한다. 새 상태를 만드는 합법 행동이 하나도 없으면 `settle` 행동만 제공하고 최대 반수와 같은 우선순위로 즉시 정산한다.

### 7.5 최대 반수 판정

48반수가 끝날 때 승자가 없으면 다음 순서로 비교한다.

1. 누적 신호 점수가 높은 플레이어
2. 현재 제어 중인 릴레이 수가 많은 플레이어
3. 전력 공급 중인 노드 수가 많은 플레이어
4. 보드 위 전체 노드 수가 많은 플레이어
5. 남은 에너지가 많은 플레이어
6. 모두 같으면 무승부

판정 기준은 로그에 `48반수 릴레이 우위`, `48반수 전력 노드 우위`처럼 구체적으로 기록한다.

## 8. 공개 상태와 AI API

### 8.1 함수 계약

```javascript
function chooseAction(state, me) {
  return state.legalActions[0];
}
```

- `me`: `"P1"` 또는 `"P2"`
- 호출 시 `state.currentPlayer === me`
- `state`를 수정하지 않음
- 비동기 함수와 `Promise` 반환 금지
- `state.legalActions`의 객체, 행동 ID 문자열 또는 같은 ID의 객체 반환

### 8.2 공개 상태 예시

```javascript
{
  gameId: "relay-forge",
  rulesVersion: "1.2.0",
  boardWidth: 5,
  boardHeight: 5,
  ply: 9,
  maxPlies: 48,
  currentPlayer: "P2",
  energy: { P1: 1, P2: 3 },
  signalScores: { P1: 3, P2: 2 },
  cores: [
    { id: "P1-core", owner: "P1", position: 22 },
    { id: "P2-core", owner: "P2", position: 2 }
  ],
  nodes: [
    { id: "P1-n1", owner: "P1", position: 21, fortified: false, powered: true },
    { id: "P1-n5", owner: "P1", position: 11, fortified: true, powered: true },
    { id: "P2-n3", owner: "P2", position: 8, fortified: false, powered: false }
  ],
  relays: [
    { position: 5, owner: null, influence: { P1: 0, P2: 1 } },
    { position: 12, owner: "P1", influence: { P1: 2, P2: 0 } },
    { position: 19, owner: null, influence: { P1: 1, P2: 0 } }
  ],
  lastActions: [
    { player: "P1", actionId: "fortify:P1-n5", text: "P1이 중앙 노드를 강화했습니다." }
  ],
  legalActions: [
    { id: "extend:7", type: "extend", to: 7, cost: 1 },
    { id: "overload:P1-n5", type: "overload", targetId: "P1-n5", cost: 2 },
    { id: "wait", type: "wait", cost: 0 }
  ]
}
```

`powered`, `relays.owner`와 `legalActions`는 엔진이 계산해 제공한다. 초급 AI가 직접 연결 그래프를 구현하지 않아도 게임에 참여할 수 있게 하기 위함이다.

### 8.3 시작용 AI

```javascript
function chooseAction(state, me) {
  const actions = state.legalActions;

  const swap = actions.find((action) => action.type === "swap");
  if (swap) return swap;

  const win = actions.find((action) => action.type === "hack");
  if (win) return win;

  const breakEnemy = actions.find((action) => action.type === "overload");
  if (breakEnemy) return breakEnemy;

  const takeRelay = actions.find((action) => action.type === "extend" &&
    state.relays.some((relay) => {
      const rowDistance = Math.abs(Math.floor(action.to / 5) - Math.floor(relay.position / 5));
      const columnDistance = Math.abs(action.to % 5 - relay.position % 5);
      return rowDistance + columnDistance === 1;
    }));
  if (takeRelay) return takeRelay;

  const extend = actions.find((action) => action.type === "extend");
  return extend || actions.find((action) => action.type === "wait");
}
```

## 9. 규칙 엔진 구조

규칙 엔진과 화면 코드를 분리한다.

```text
relay/
├─ index.html
├─ styles.css
├─ app.js                 화면, 입력, 대전 진행
├─ engine.js              상태, 합법 행동, 적용, 판정
├─ ai.js                  내장 AI v1~v5
├─ custom-ai-runner.js    사용자 코드 검증과 Worker 호출
├─ custom-ai-worker.js    격리 실행
├─ battle-log.js          로그와 다운로드
├─ example-ais/           복사 가능한 단계별 AI
└─ tests/
   ├─ engine.test.js
   ├─ ai.test.js
   ├─ custom-ai.test.js
   └─ battle-log.test.js
```

### 9.1 필수 엔진 함수

```javascript
initialState()
cloneState(state)
poweredNodes(state, player)
relayControl(state)
legalActions(state, player)
applyAction(state, action)
outcome(state)
stateKey(state)
```

`applyAction`은 입력 상태를 변경하지 않고 새 상태를 반환한다. UI, 내장 AI와 사용자 AI 검증은 동일한 `legalActions`를 사용한다.

### 9.2 행동 적용 순서

```text
행동 ID 검증
→ 비용 차감
→ 보드 변경
→ 양측 전력망 재계산
→ 릴레이 제어 재계산
→ 행동 플레이어의 signalScores 누적
→ P2 응답 뒤 신호 승부 판정
→ 해킹/초패/교착/최대 반수 판정
→ 차례 전환
```

이 순서를 테스트와 LLM용 규격에 동일하게 명시한다.

## 10. 예제 AI 단계

| 버전 | 방식 | 핵심 학습 요소 |
|---|---|---|
| v1 | 합법 행동 무작위 | API와 자동 대전 이해 |
| v2 | 규칙 우선순위 | 해킹, 릴레이, 과부하 우선 |
| v3 | 2반수 미니맥스 | 상대의 절단과 해킹 위협 방어 |
| v4 | 4반수 알파베타 + 전치표 | 상태 키와 탐색 최적화 |
| v5 | 반복 심화 + 연결 그래프 평가 | 절단점, 복수 경로와 시간 관리 |

### 10.1 권장 평가 함수 특징

```text
즉시 승리·패배
+ 누적 신호 점수
+ 제어 릴레이 수
+ 상대 코어까지의 최단 확장 거리
+ 전력 공급 노드 수
+ 코어에서 릴레이까지의 복수 연결 경로
+ 에너지
+ 강화된 핵심 절단점
- 상대의 다음 수 해킹 가능성
- 비활성 노드 수
- 한 번의 과부하로 끊길 노드 수
```

v5는 단순 노드 개수보다 **연결망의 절단점과 우회 경로**를 높게 평가해야 한다.

## 11. 사용자 AI 실행과 보안

| 항목 | 규격 |
|---|---|
| 코드 크기 | 최대 50,000바이트 |
| 행동 제한 시간 | 500ms |
| 경기 제한 | 48반수 |
| 실행 위치 | 별도 Web Worker |
| 상태 | 구조화 복제 후 깊게 동결 |
| 반환 검증 | 행동 ID를 현재 `legalActions`와 대조 |

다음 상황은 사용자 AI 오류로 기록한다.

- `chooseAction` 함수 누락
- 문법 오류 또는 실행 예외
- 500ms 초과
- `Promise` 반환
- 빈 값 또는 잘못된 행동 반환
- 현재 합법 행동에 없는 ID 반환
- 50KB를 초과하는 코드 또는 파일

네트워크, DOM, 저장소 관련 API는 Worker 안에서 사용할 수 없게 제한한다. UI에는 브라우저 Worker가 신뢰할 수 없는 코드를 위한 완전한 보안 경계가 아니라는 안내를 표시한다.

## 12. 공정 대전 방식

### 12.1 기본 벤치마크

- 상대 AI 하나당 최소 10경기
- 사용자 AI의 P1·P2를 같은 횟수로 교대
- 무작위성이 없으므로 같은 버전의 초기 상태 사용
- 엔진 버전과 양측 AI 버전을 경기 로그에 기록
- 승·무·패와 오류를 별도로 집계

결정론적 AI끼리는 같은 진영에서 결과가 반복될 수 있다. 다양한 평가를 위해 다음 대칭 시작 변형을 선택적으로 제공할 수 있다.

- 기본형: 양측 기본 노드 `1·3`, `21·23`
- 좌측형: 각 플레이어의 기본 노드 하나를 왼쪽 안쪽으로 이동
- 우측형: 좌측형을 좌우 반전

시작 변형은 규칙에 등록된 고정 ID만 사용하며, 대전에서 양 진영에 대칭적으로 배정한다. MVP에서는 기본형만 구현한다.

### 12.2 경기 로그

각 경기 로그에는 다음을 포함한다.

```text
gameId / rulesVersion
matchId / 시작 변형 ID
P1 AI / P2 AI / 사용자 진영
제한 시간 / 최대 반수
각 반수의 에너지, 행동 ID와 행동 설명
행동 후 릴레이 제어와 누적 신호 점수
사용자 AI 실행 시간
승자 / 종료 사유 / 오류 유형
```

## 13. 화면 구성

### 13.1 메인 보드

- 5×5 회로판
- P1·P2 코어와 중앙 릴레이 3개를 고유한 모양으로 표시
- 전력 노드는 밝게, 비활성 노드는 흐리게 표시
- 강화 노드에는 방패 테두리 표시
- 마지막 설치·과부하·강화 위치 강조
- 선택한 행동에 필요한 에너지 표시

### 13.2 정보 영역

- 현재 플레이어와 반수
- 양측 에너지 `현재/4`
- 릴레이 소유 현황
- 누적 신호 점수 `현재/5`
- 행동 로그와 종료 사유

### 13.3 플레이 모드

- 사람 대 예제 AI
- 예제 AI끼리 한 경기
- 사용자 AI 코드 1행동 검증
- 사용자 AI 대 v1~v5 자동 대전
- 경기별 리플레이

### 13.4 코드 영역

- 기본 `chooseAction` 예제
- LLM용 전체 규격 복사
- `.js`·`.txt` 파일 불러오기
- 예제 복원, 코드 검증, 대전 시작과 중지
- `50KB · 500ms/행동` 상시 표시

## 14. 자동 테스트 승인 기준

### 14.1 초기 상태와 연결

- [ ] 초기 상태가 좌우·상하 대칭이다.
- [ ] 양측 코어와 기본 노드가 모두 전력 공급 상태다.
- [ ] 초기 합법 행동에 연결, 강화, 대기가 포함된다.
- [ ] 대각선 노드는 연결된 것으로 계산하지 않는다.
- [ ] 중간 노드 제거 시 뒤쪽 노드가 비활성화된다.
- [ ] 우회 경로가 있으면 한 노드가 제거되어도 전력이 유지된다.

### 14.2 행동

- [ ] 연결은 전력 노드에 인접한 빈칸만 허용한다.
- [ ] 에너지가 부족한 행동은 생성되지 않는다.
- [ ] 일반 노드는 전력 노드 2개, 강화 노드는 3개로 과부하할 수 있다.
- [ ] 비활성 노드는 포위 수에 포함되지 않는다.
- [ ] 비활성 노드는 강화하거나 해킹에 사용할 수 없다.
- [ ] 코어는 과부하 대상이 되지 않는다.
- [ ] P2에게만 첫 행동 직후 `swap`이 한 번 제공되고 진영 전체가 180도 교환된다.
- [ ] 릴레이 칸에는 노드를 설치할 수 없다.
- [ ] 원본 상태는 `applyAction` 후에도 변경되지 않는다.

### 14.3 승리와 종료

- [ ] 인접한 전력 노드와 에너지 3으로 해킹하면 즉시 승리한다.
- [ ] 자기 행동 뒤 보유 릴레이 수만큼 신호 점수를 얻는다.
- [ ] P2 응답 뒤 한쪽만 5점 이상이면 승리하고 동점이면 무승부다.
- [ ] 릴레이 영향력은 주변 활성 노드만 세며 최소 2, 동률은 중립이다.
- [ ] 이미 얻은 신호 점수는 릴레이를 잃어도 유지된다.
- [ ] 과거 전체 상태를 재현하는 행동은 합법 행동에서 제외된다.
- [ ] 새 상태를 만들 수 없으면 `settle`로 즉시 정산한다.
- [ ] 48반수 판정의 비교 순서가 정확하다.
- [ ] 완전 동률은 무승부로 끝난다.

### 14.4 사용자 AI와 대전

- [ ] 공개 상태는 JSON 복제 가능하고 깊게 동결된다.
- [ ] 합법 행동 ID와 일치하는 반환값만 허용한다.
- [ ] 무한 루프를 500ms 안에 종료한다.
- [ ] `Promise`, 예외와 50KB 초과 코드를 거부한다.
- [ ] P1·P2 경기 수가 동일하게 배정된다.
- [ ] 경기 로그로 모든 상태 변화를 재현할 수 있다.

## 15. 밸런스 검증 지표

MVP 구현 후 AI 자동 대전으로 다음 목표를 확인한다.

| 지표 | 목표 범위 |
|---|---:|
| 평균 경기 길이 | 12~20반수 |
| 48반수 도달률 | 5% 이하 |
| 초기 합법 행동 수 | 6~12개 |
| 일반 상태 합법 행동 수 | 5~30개 |
| 결정 경기 중 P1 승률 | 45~55% |
| 해킹 승리 비율 | 추후 AI 다양화 지표 |
| 신호 점수 승리 비율 | 추후 AI 다양화 지표 |
| 무승부 비율 | 20~45% |

해킹 승리가 지나치게 많으면 해킹 비용이나 코어 인접 칸을 조정한다. 신호 승리가 지나치게 빠르면 목표 점수를 `6` 이상으로 올린다. 한 번에 두 수치를 함께 바꾸지 않고 버전별 벤치마크를 남긴다.

규칙 1.2.0의 내장 AI v2~v5 교차 대전 16경기는 `P1 5승 · P2 5승 · 무승부 6회`, 경기 길이 `12~14반수`, 반복·48반수 종료 `0회`였다.

## 16. MVP 구현 순서

1. 순수 규칙 엔진과 단위 테스트
2. 5×5 직접 플레이 UI
3. v1 무작위와 v2 규칙형 AI
4. 사용자 AI Worker와 1행동 검증
5. 선·후공 자동 대전과 결과 집계
6. 경기 로그와 리플레이
7. v3~v5 탐색 AI와 텔레메트리
8. 모바일 UI와 접근성 점검
9. 밸런스 벤치마크 후 `rulesVersion 1.2.0` 고정

## 17. MVP 제외 범위

다음 기능은 첫 공개 버전에 포함하지 않는다.

- 온라인 실시간 대전
- 계정, 랭킹과 서버 저장
- 커스텀 맵 편집기
- 세 번째 플레이어 또는 팀전
- 노드별 추가 능력과 아이템
- 애니메이션 스킨 구매 요소

규칙과 AI 계약이 안정된 뒤 시작 변형, 주간 챌린지와 토너먼트 내보내기를 별도 버전으로 검토한다.

## 18. AIArena 조건 충족 확인

| 요구 속성 | Relay Forge 설계 |
|---|---|
| 유한한 턴제 | 48반수 상한, 전체 상태 초패와 교착 정산 |
| 결정론적 엔진 | 무작위 요소 없음 |
| JSON 공개 상태 | 보드·노드·에너지·릴레이·합법 행동 제공 |
| 제한된 행동 공간 | 연결망의 경계와 현재 에너지로 행동 생성 |
| 복수 승리 경로 | 코어 해킹 또는 릴레이 신호 5점 선취 |
| 전략적 상충 | 확장·절단·강화·에너지 비축 |
| 공정한 평가 | 동일 초기 상태, P1·P2 교대 |
| 안전 실행 | 50KB, 500ms, Worker, 깊은 동결과 재검증 |
| 관찰 가능성 | 전력 표시, 행동 로그, 종료 사유와 리플레이 |
| 단계별 학습 | 무작위부터 연결 그래프 탐색까지 v1~v5 |

---

문서 버전: 1.2  
게임 규칙: 1.2.0  
작성일: 2026-08-13
