# AIArena 예제 게임 필수 속성

## 1. 목적과 적용 범위

이 문서는 AIArena에 새로운 예제 게임을 추가할 때 필요한 게임 설계 속성, AI 입출력 계약, 실행 안전장치, 대전 평가 방식과 사용자 경험을 정의한다.

예제 게임은 단순히 브라우저에서 플레이할 수 있는 게임이 아니라 다음 흐름을 완결해야 한다.

> 사용자가 규칙을 이해한다 → AI 함수를 작성한다 → 합법성을 검증한다 → 공정하게 대전한다 → 로그와 결과를 보고 AI를 개선한다

이 문서에서 **필수**는 새 게임을 AIArena에 공개하기 전에 반드시 구현해야 하는 항목이고, **권장**은 게임의 학습성과 비교 가능성을 높이는 항목이다.

## 2. 한눈에 보는 필수 속성

| 구분 | 필수 속성 | 완료 조건 |
|---|---|---|
| 게임 구조 | 유한한 턴제 진행 | 최대 턴 또는 반복 판정으로 모든 경기가 종료됨 |
| 규칙 엔진 | UI와 분리된 결정론적 엔진 | 같은 상태와 행동이 같은 다음 상태를 만듦 |
| 상태 | 직렬화 가능한 공개 상태 | AI가 DOM이나 전역 변수 없이 판단 가능 |
| 행동 | 엔진이 만든 합법 행동 목록 | AI는 `legalActions` 중 하나만 선택 |
| 판정 | 명시적인 승·패·무승부와 종료 사유 | 모든 종료 경로가 기계적으로 판정됨 |
| AI 계약 | 동기식 단일 함수 | `chooseAction(state, me)`가 행동 하나를 반환 |
| 안전 | 격리 실행과 자원 제한 | Worker, 시간 제한, 코드 크기 제한, 반환값 재검증 |
| 공정성 | 선·후공 교대와 동일 조건 | 같은 상대와 양 진영에서 동일 횟수 대전 |
| 재현성 | 시드 또는 초기 조건 기록 | 같은 경기의 재실행·원인 분석이 가능 |
| 관찰성 | 행동 로그, 결과, 리플레이 | 사용자가 AI의 선택과 패인을 확인 가능 |
| 학습성 | 난이도별 예제 AI | 무작위부터 탐색형까지 단계별 비교 가능 |
| 문서화 | 규칙과 전체 AI 규격 제공 | LLM이나 사람이 복사해 바로 코드를 작성 가능 |

## 3. 게임 설계 속성

### 3.1 완전 관찰 또는 명시적인 정보 공개 범위 — 필수

AI가 볼 수 있는 정보는 하나의 공개 상태 객체에 모두 들어 있어야 한다. 숨은 정보가 있는 게임이면 각 플레이어에게 전달되는 정보의 범위가 규칙으로 고정되어야 하며, 상대의 비공개 정보가 노출되어서는 안 된다.

- 보드, 유닛, 카드, 자원, 점수와 현재 차례를 명시한다.
- 좌표 체계와 플레이어별 진행 방향을 문서화한다.
- 객체에는 경기 중 변하지 않는 안정적인 ID를 부여한다.
- 상태는 JSON으로 복제할 수 있는 데이터만 사용한다.
- UI 선택 상태, DOM 요소, 함수, 클래스 인스턴스는 공개 상태에 넣지 않는다.

### 3.2 제한된 선택지와 명확한 결과 — 필수

규칙 엔진이 현재 선택할 수 있는 행동을 전부 생성해야 한다. AI가 좌표나 명령을 임의 조립하게 하지 않고, `state.legalActions`에 있는 행동 하나를 고르게 한다.

- 권장 평균 행동 수는 5~40개이다.
- 행동 수가 자주 100개를 넘으면 목표 후보를 압축하거나 계층형 선택을 검토한다.
- 행동 하나가 다음 상태에 의미 있는 변화를 만들어야 한다.
- 동일한 합법 행동에는 재현 가능한 `id`를 부여하는 것을 권장한다.
- 패스가 필요하면 언제 가능한지와 연속 패스 처리 규칙을 명시한다.

### 3.3 유한하고 전략적인 경기 길이 — 필수

브라우저 자동 대전에 적합하도록 종료 상한을 둔다. 한 경기는 사람이 리플레이로 따라갈 수 있을 만큼 짧되, 즉시 이득과 장기 계획이 모두 존재해야 한다.

- 최대 턴 또는 최대 반수를 둔다.
- 반복 상태, 행동 불가, 시간 초과의 판정 방식을 정의한다.
- 일반적인 경기는 약 20~60번의 의사결정 안에 끝나는 것을 권장한다.
- 한 행동의 영향이 지나치게 작거나 한 번의 무작위 결과가 전체 승패를 결정하지 않게 한다.

### 3.4 복수의 승리 경로와 전략적 상충 — 권장

단일 전술만 반복하는 AI보다 다양한 평가 함수를 만들 수 있어야 한다.

- 즉시 목표: 포획, 피해, 카드 사용 효율
- 중기 목표: 위치, 진형, 자원, 공격로 장악
- 최종 목표: 핵심 유닛·건물 파괴, 도달, 점수 우위
- 공격과 방어, 현재 이득과 미래 준비 사이에 실제 선택 비용이 존재해야 한다.

### 3.5 통제 가능한 무작위성 — 필수

무작위 요소가 있으면 경기 시드를 입력받아 같은 조건을 재현할 수 있어야 한다.

- 엔진 내부에서 직접 `Math.random()`에 의존하지 않고 RNG를 주입한다.
- 대전 로그에 시드와 초기 배치를 기록한다.
- 비교 대전에서는 양측이 대칭적인 시드 묶음을 사용한다.
- 무작위성이 없는 게임도 동점 행동의 선택 규칙을 고정한다.

## 4. 표준 AI 계약

새 예제 게임은 다음 공통 이름을 사용하는 것을 권장한다. 기존 동물장기의 `chooseMove`는 호환성을 위해 유지할 수 있지만, 신규 게임은 `chooseAction`으로 통일한다.

```javascript
function chooseAction(state, me) {
  return state.legalActions[0];
}
```

### 4.1 입력 계약 — 필수

공개 상태의 공통 최상위 속성은 다음과 같다.

| 속성 | 형식 | 설명 |
|---|---|---|
| `gameId` | `string` | 게임 규격 식별자 |
| `rulesVersion` | `string` | 규칙과 상태 스키마 버전 |
| `turn` 또는 `ply` | `number` | 현재 진행 번호 |
| `currentPlayer` | `"P1" \| "P2"` | 현재 행동할 플레이어 |
| `maxTurns` 또는 `maxPlies` | `number` | 경기 종료 상한 |
| `board` 또는 `units` | JSON 값 | 현재 공간과 객체 배치 |
| `resources` | JSON 객체 | 손패, 에너지, 보유 말 등 판단 자원 |
| `lastActions` | 배열 | 직전 상태 변화 요약; 없으면 빈 배열 |
| `legalActions` | 행동 배열 | 현재 반환 가능한 모든 행동 |

게임 특성상 필요 없는 속성은 생략할 수 있지만, 현재 차례와 진행 번호, 판단에 필요한 전체 공개 정보, 합법 행동 목록은 반드시 제공해야 한다.

`me`는 항상 `"P1"` 또는 `"P2"`이고 호출 시점에는 `state.currentPlayer === me`여야 한다. 플레이어 방향이 달라도 상태 구조와 좌표 체계는 바꾸지 않는다.

### 4.2 행동 계약 — 필수

모든 행동은 최소한 `id`와 `type`을 갖는 것을 권장한다.

```javascript
{ id: "move:u1:18", type: "move", unitId: "u1", to: 18 }
{ id: "play:iron_guard:36", type: "play", cardId: "iron_guard", position: 36 }
{ id: "pass", type: "pass" }
```

- AI는 `legalActions`의 객체, 그 객체의 `id`, 또는 동일한 `id`를 가진 객체를 반환할 수 있다.
- 엔진은 반환값을 반드시 현재 합법 행동 목록과 다시 대조한다.
- 누락, 잘못된 타입, 존재하지 않는 ID는 해당 경기의 AI 오류 또는 패배로 처리한다.
- `Promise` 반환과 비동기 작업은 허용하지 않는다.
- 전달받은 `state`는 읽기 전용이다.

### 4.3 규칙 엔진 계약 — 필수

UI와 AI가 함께 사용할 순수 규칙 계층에는 다음 기능이 필요하다.

```text
initialState(options)          초기 상태 생성
cloneState(state)             상태 복제
legalActions(state, player)   합법 행동 열거
applyAction(state, action)    다음 상태 생성
outcome(state)                승자와 종료 사유 반환
stateKey(state)               반복 판정·재현용 키
```

`applyAction`은 원본 상태를 변경하지 않고 새 상태를 반환해야 한다. 내부 최적화를 위해 변경형 구현을 사용하더라도 공개 API에서는 복제와 검증 경계를 보장해야 한다.

`outcome`의 권장 반환 형식은 다음과 같다.

```javascript
{ winner: "P1", reason: "P2 Core 파괴" }
{ winner: null, reason: "최대 턴 완전 동률" }
```

진행 중이면 `null`을 반환한다. 종료 사유는 리플레이와 경기 로그에서 그대로 사용할 수 있는 문장이어야 한다.

## 5. 사용자 AI 실행 속성

### 5.1 격리와 제한 — 필수

AIArena의 현재 기준값은 다음과 같다.

| 항목 | 기준 |
|---|---:|
| 코드 크기 | 최대 50,000바이트 |
| 행동 시간 | 최대 500ms |
| 실행 방식 | 매 호출 또는 경기별 Web Worker |
| 반환 방식 | 동기식 단일 행동 |
| 상태 전달 | 복제본 또는 구조화 복제 |
| 사후 검증 | 현재 `legalActions`와 대조 |

추가 안전 요구사항은 다음과 같다.

- 제한 시간 초과 시 Worker를 종료한다.
- 실행 오류, 시간 초과, 불법 행동을 서로 구분해 기록한다.
- 가능하면 상태를 깊게 동결해 사용자 코드의 변경을 막는다.
- 네트워크, DOM, 저장소와 통신 API를 노출하지 않는다.
- 브라우저 Worker는 완전한 보안 경계가 아니라고 사용자에게 알린다.
- 출처를 모르는 코드와 비밀번호·토큰이 들어 있는 코드를 실행하지 말라는 경고를 제공한다.

### 5.2 오류 처리 — 필수

다음 상황은 UI와 경기별 로그에 명확한 원인으로 남겨야 한다.

- 필수 함수가 없음
- 문법 또는 실행 오류
- 시간 초과
- 비동기 값 반환
- 빈 값 또는 잘못된 형식 반환
- 현재 합법 행동에 없는 값 반환
- 코드 또는 파일 크기 초과

여러 경기를 묶어 평가할 때 한 경기의 오류가 전체 앱을 중단시키지 않아야 한다. 오류 경기의 처리 원칙은 모든 AI에 동일하게 적용한다.

## 6. 공정한 대전과 평가

### 6.1 매치 구성 — 필수

- 동일 상대에 대해 사용자 AI의 P1·P2 경기 수를 같게 한다.
- 무작위 초기 조건은 양 진영에 대칭적으로 재사용한다.
- 엔진 버전, AI 코드 해시, 상대 AI 버전, 시드와 제한값을 기록한다.
- 최소 10경기를 권장하며, 짧은 게임은 더 많은 표본을 사용한다.
- 승·무·패와 오류 수를 별도로 집계한다.

### 6.2 종료 판정 — 필수

각 게임은 다음 경로를 모두 정의해야 한다.

- 규칙상 즉시 승리 또는 패배
- 최대 턴 도달 시 비교 순서
- 완전 동률 또는 반복에 의한 무승부
- 행동 불가 상태
- AI 실행 오류, 시간 초과와 불법 행동

최대 턴 판정에서 보조 점수를 쓸 경우 우선순위를 고정한다. 예를 들어 `파괴 점수 → 타워 체력 → 유닛 체력 → 무승부`처럼 문서와 코드가 같은 순서를 사용해야 한다.

### 6.3 예제 AI 단계 — 권장

학습용 예제는 강함뿐 아니라 알고리즘의 차이를 보여줘야 한다.

| 단계 | 구현 예 | 학습 목표 |
|---|---|---|
| v1 | 합법 행동 무작위 | 함수 계약과 대전 흐름 |
| v2 | 한 수 정적 평가 | 특징과 가중치 설계 |
| v3 | 얕은 미니맥스·롤아웃 | 상대 수 고려 |
| v4 | 알파베타·전치표 | 탐색 효율 |
| v5+ | 반복 심화·테이블베이스·도메인 전략 | 강한 AI와 한계 관리 |

각 AI는 이름, 알고리즘, 탐색 깊이 또는 계산 예산, 예상 난이도를 표시한다.

## 7. 사용자 경험과 문서 속성

### 7.1 게임 화면 — 필수

- 현재 차례, 양측 이름, 핵심 자원과 승리 목표를 항상 표시한다.
- 선택 가능한 행동과 마지막 행동을 시각적으로 구분한다.
- 행동 결과를 애니메이션 또는 순서가 있는 로그로 설명한다.
- 승자뿐 아니라 종료 사유를 표시한다.
- 사용자가 직접 플레이해 규칙을 익힐 수 있게 한다.

### 7.2 코드 작성 화면 — 필수

- 함수 시그니처와 최소 예제를 편집기에 기본 제공한다.
- 전체 LLM용 규격을 한 번에 복사할 수 있게 한다.
- 코드 초기화, 1행동 검증, 대전 시작과 중지 기능을 제공한다.
- 코드 크기와 행동 시간 제한을 편집기 근처에 표시한다.
- `.js` 또는 `.txt` 불러오기는 권장 기능이다.

### 7.3 결과와 관찰성 — 필수

- 전체 승·무·패, 승률, 오류 수를 표시한다.
- 상대 AI와 진영별 결과를 구분할 수 있어야 한다.
- 각 경기의 시드, 행동 목록, 결과와 종료 사유를 보관한다.
- 마지막 경기 또는 선택한 경기의 리플레이를 제공한다.
- UTF-8 텍스트나 JSON으로 로그를 복사·다운로드할 수 있게 하는 것을 권장한다.

## 8. 현재 예제 게임의 실제 속성

### 8.1 동물장기 (`/animal/`)

| 항목 | 현재 구현 |
|---|---|
| AI 함수 | `chooseMove(state, me)` |
| 공개 상태 | `board`, `hands`, `turn`, `ply`, `legalMoves` |
| 보드 | 3×4, 길이 12의 1차원 배열 |
| 말 | `{ owner, type }`; `lion`, `giraffe`, `elephant`, `chick`, `hen` |
| 행동 | `{ type:"move", from, to }`, `{ type:"drop", piece, to }` |
| 승리 | 라이온 포획 또는 트라이 성공 |
| 무승부 | 같은 국면 3회 반복, 대전 시 최대 240반수 |
| 실행 제한 | 코드 50KB, 한 수 500ms, Web Worker |
| 검증 | 상태 복제·동결, 제한 API 차단, 합법 수 재검증 |
| 평가 | v1~v5, 상대별 선·후공 동일 배정, 경기별 로그 |

### 8.2 ClashCard (`/card/`)

| 항목 | 현재 구현 |
|---|---|
| AI 함수 | `chooseAction(state, me)` |
| 공개 상태 | `boardWidth`, `boardHeight`, `turn`, `currentPlayer`, `maxTurns`, `energy`, `hands`, `nextCards`, `units`, `towers`, `river`, `bridges`, `cards`, `lastActions`, `legalActions` |
| 보드 | 12×7, 길이 84의 좌표 공간 |
| 행동 | `{ id, type:"play", cardId, position }`, `{ id:"pass", type:"pass" }` |
| 승리 | Core 파괴 또는 상대 타워 전멸 |
| 최대 턴 판정 | 파괴 점수 → 타워 체력 → 유닛 체력 → 무승부 |
| 실행 제한 | 한 행동 500ms, Web Worker |
| 평가 | 강한 AI V12와 1·10·30경기, 선·후공 교대, 리플레이 |
| 무작위 요소 | 경기마다 양측 덱을 독립적으로 섞음 |

## 9. 현재 구현에서 통일이 필요한 항목

신규 예제를 추가하기 전에 다음 공통 규격을 우선 적용한다.

1. 신규 게임의 AI 함수 이름을 `chooseAction(state, me)`로 통일한다.
2. 모든 행동에 안정적인 `id`를 부여하고 ID로 합법성을 검증한다.
3. 공개 상태에 `gameId`, `rulesVersion`, 현재 플레이어와 종료 상한을 넣는다.
4. 무작위 게임은 시드 기반 RNG와 시드 로그를 사용한다.
5. 코드 크기 50KB, 500ms, 동기 반환, 깊은 상태 동결을 모든 게임에 동일하게 적용한다.
6. 사용자에게 공개되는 정보와 숨은 정보를 게임별 규칙에 명시한다.
7. 오류를 경기 패배로만 합산하지 말고 오류 유형과 횟수를 별도로 표시한다.
8. 경기 로그에 규칙 버전, 진영, 상대 버전, 시드, 종료 사유를 포함한다.

현재 ClashCard는 코드 크기 검사, 공개 상태 깊은 동결, 제한 API 차단과 시드 재현성을 보강해야 한다. 또한 `hands`와 `nextCards`를 양측 모두 공개하는 현재 방식은 완전 관찰 게임 규칙으로 명시하거나, 비공개 카드 게임으로 만들 경우 상대 정보를 가려야 한다.

## 10. 신규 예제 게임 승인 체크리스트

### 게임과 규칙

- [ ] 게임 상태가 유한하고 최대 턴 또는 반복 판정이 있다.
- [ ] 승리, 패배, 무승부, 행동 불가의 판정이 모두 정의되어 있다.
- [ ] 같은 상태와 행동은 같은 다음 상태를 만든다.
- [ ] 무작위 요소는 시드로 재현할 수 있다.
- [ ] 직접 플레이만으로 핵심 규칙을 확인할 수 있다.

### 상태와 행동

- [ ] AI 판단에 필요한 공개 정보가 하나의 JSON 상태에 들어 있다.
- [ ] 현재 플레이어, 진행 번호와 종료 상한이 포함되어 있다.
- [ ] 엔진이 모든 합법 행동을 생성한다.
- [ ] 각 행동에 안정적인 ID가 있다.
- [ ] AI 반환값을 현재 합법 행동과 다시 대조한다.
- [ ] 플레이어 방향에 따라 상태 스키마가 달라지지 않는다.

### 실행 안전성

- [ ] 사용자 코드는 Web Worker에서 실행된다.
- [ ] 코드 크기 50KB와 행동 시간 500ms 제한이 적용된다.
- [ ] 전달 상태는 복제되고 가능하면 깊게 동결된다.
- [ ] Promise 반환, 예외, 시간 초과와 불법 행동을 처리한다.
- [ ] 네트워크, DOM과 저장소 API 사용을 제한한다.
- [ ] 신뢰할 수 없는 코드 실행에 대한 경고가 있다.

### 평가와 학습

- [ ] 선·후공을 같은 횟수로 배정한다.
- [ ] 무작위 초기 조건은 공정하게 짝지어 사용한다.
- [ ] 승·무·패와 오류를 상대별로 집계한다.
- [ ] 최소한 무작위·휴리스틱·탐색형 예제 AI가 있다.
- [ ] 경기별 행동 로그와 종료 사유가 남는다.
- [ ] 리플레이 또는 단계별 상태 확인이 가능하다.

### 문서와 품질

- [ ] 함수, 상태, 행동, 규칙, 제한을 포함한 LLM용 규격이 있다.
- [ ] 바로 실행 가능한 기본 AI 코드가 있다.
- [ ] 규칙 엔진, 합법 행동, 종료 판정과 AI 오류의 자동 테스트가 있다.
- [ ] 모바일을 포함한 주요 화면 크기에서 보드와 편집기를 사용할 수 있다.
- [ ] 브라우저 콘솔 오류 없이 한 경기와 다경기 대전이 완료된다.

---

문서 버전: 1.0  
기준 구현: AIArena `/animal/`, `/card/`  
작성일: 2026-08-13
