import{applyAction,initialState,outcome}from"./engine.js";import{chooseAction}from"./ai.js";const levels=["v2","v3","v4","v5"],r={P1:0,P2:0,DRAW:0,plies:0,games:0};for(const seed of Array.from({length:12},(_,i)=>i+1))for(const p1 of levels)for(const p2 of levels){let s=initialState({seed});while(!outcome(s))s=applyAction(s,chooseAction(s,s.currentPlayer==="P1"?p1:p2).action);r[outcome(s).winner??"DRAW"]++;r.plies+=s.ply;r.games++}console.log(`Chrono Courier ${r.games}경기 · P1 ${r.P1} · P2 ${r.P2} · 무 ${r.DRAW} · 평균 ${(r.plies/r.games).toFixed(1)}행동`);
