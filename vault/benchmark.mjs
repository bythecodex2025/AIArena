import { applyAction, initialState, outcome } from "./engine.js"; import { chooseAction } from "./ai.js";
const levels=["v2","v3","v4","v5"]; const result={P1:0,P2:0,DRAW:0,plies:0,games:0}; for(const seed of Array.from({length:100},(_,i)=>i+1))for(const p1 of levels)for(const p2 of levels){let state=initialState({seed});while(!outcome(state))state=applyAction(state,chooseAction(state,state.currentPlayer==="P1"?p1:p2).action);const end=outcome(state);result[end.winner??"DRAW"]+=1;result.plies+=state.ply;result.games+=1;} console.log(`Echo Vault ${result.games}경기 · P1 ${result.P1} · P2 ${result.P2} · 무 ${result.DRAW} · 평균 ${(result.plies/result.games).toFixed(1)}행동`);
